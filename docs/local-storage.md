# Local Storage

The web beta uses a schema-versioned localStorage repository with Zod validation and safe fallback. It stores policies, structured history, subscriptions, theme, and onboarding state. Clear and export boundaries are exposed through settings and the repository interface.

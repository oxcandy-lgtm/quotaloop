# Known Limitations

- Verified real quota reads are unavailable for initial CLI adapters.
- Web and desktop state synchronization is local-shell groundwork only.
- Launch at login is documented but not enabled in the unsigned beta shell.
- Production signal collection, cloud sync, remote control, billing, and signing are excluded.
- Native notification delivery requires interactive OS permission and manual verification.

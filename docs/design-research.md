# Design Research

The interface follows general local-first design principles:

- compact provider status hierarchy and reset timing;
- modular menu-bar information density;
- local ownership and watcher separation.

QuotaLoop uses its own layout, component structure, language, and visual system.
